import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hipstagram.app',
  appName: 'Hipstagram',
  webDir: 'dist/frontend/browser'
};

export default config;
